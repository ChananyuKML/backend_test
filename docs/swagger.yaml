host: localhost:8000
info:
  contact: {}
  description: Authentication API with JWT & Refresh Token
  title: Hole Auth API
  version: "1.0"
paths: {}
securityDefinitions:
  BearerAuth:
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
